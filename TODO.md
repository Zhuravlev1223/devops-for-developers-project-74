# TODO - Fix Hexlet CI Failure (approved plan)

## Steps:
1. [x] Remove obsolete `version:` from docker-compose.yml and docker-compose.override.yml
2. [x] Create `code/` directory 
3. [x] Create `code/.env.example` with DB vars from README
4. [x] Create `code/Makefile` with code-setup target
5. [x] Create `code/docker-compose.yml` 

7. [ ] Local test
8. [ ] Complete
